import { TipoDocumentoDirective } from './tipo-documento.directive';

describe('TipoDocumentoDirective', () => {
  it('should create an instance', () => {
    const directive = new TipoDocumentoDirective();
    expect(directive).toBeTruthy();
  });
});
